# CẤU HÌNH ỨNG DỤNG
apiVersion: v1
kind: ConfigMap
metadata:
  name: uniform-web-update-configmap
  namespace: uniform-test
data:
  GATEWAY_URL: "https://uniform-gateway-test.lienvietpostbank.com.vn"


---
# BẢO VỆ MẬT KHẨU
apiVersion: v1
kind: Secret
metadata:
  name: uniform-web-update-secret
  namespace: uniform-test
type: Opaque
data:
  k1: "djE="


---
# DEPLOY CÁC INSTANCE
apiVersion: apps/v1
kind: Deployment
metadata:
  name: uniform-web-update
  namespace: uniform-test
  labels:
    app: uniform-web-update
spec:
  replicas: 1
  selector:
    matchLabels:
      app: uniform-web-update
  template:
    metadata:
      labels:
        app: uniform-web-update
    spec:
      nodeName: k8s-uni-node-187
      containers:
        - name: uniform-web-update
          image: harbor-registry.lienvietpostbank.com.vn:5443/uniform/uniform-web-update:test
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          env:
            - name: GATEWAY_URL
              valueFrom:
                configMapKeyRef:
                  name: uniform-web-update-configmap
                  key: GATEWAY_URL
            - name: e2
              valueFrom:
                secretKeyRef:
                  name: uniform-web-update-secret
                  key: k1
          resources:
            requests:
              memory: '1024Mi'
              cpu: '0.5'
            limits:
              memory: '2048Mi'
              cpu: '1'

          livenessProbe:
            httpGet:
              path: "/"
              port: 80
              httpHeaders:
                - name: "health-token"
                  value: "eyJzdWIiOiJhY3R1YXRvciIsIm5hbWUiOiJoZWFsdGhjaGVjayJ9"
            periodSeconds: 30
            failureThreshold: 10
            timeoutSeconds: 30
            initialDelaySeconds: 90
            successThreshold: 1

          readinessProbe:
            httpGet:
              path: "/"
              port: 80
              httpHeaders:
                - name: "health-token"
                  value: "eyJzdWIiOiJhY3R1YXRvciIsIm5hbWUiOiJoZWFsdGhjaGVjayJ9"
            periodSeconds: 30
            failureThreshold: 3
            timeoutSeconds: 30
            initialDelaySeconds: 60
            successThreshold: 1

---
# CẤU HÌNH SERVICE
apiVersion: v1
kind: Service
metadata:
  name: uniform-web-update
  namespace: uniform-test
spec:
  selector:
    app: uniform-web-update
  ports:
    - name: http
      port: 80


---
# CẤU HÌNH INGRESS
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: uniform-web-update-ingress
  namespace: uniform-test
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: "uniform-test-update.lienvietpostbank.com.vn"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: uniform-web-update
                port:
                  number: 80

---
# AUTO SCALE
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: uniform-web-update
  namespace: uniform-test
spec:
  scaleTargetRef:
    apiVersion: apps/v1beta1
    kind: Deployment
    name: uniform-web-update
  minReplicas: 1
  maxReplicas: 2
  metrics:
  - type: Resource
    resource:
      name: cpu
      targetAverageUtilization: 70
