import { MaxSelectedDisplayPipe } from './max-selected-display.pipe';

describe('MaxSelectedDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new MaxSelectedDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
